A text input, label and hint included.

```jsx
<Field label="Sleeper username" placeholder="tkeefe" hint="We read your leagues; we never post." />
<Field label="Monthly budget" type="number" defaultValue={25} />
```

The label doubles as the `id`/`for` pairing. There is one input treatment — no filled or underlined variant.
