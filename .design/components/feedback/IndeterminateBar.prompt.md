The one loading indicator — a single indeterminate segment on a hairline track.

```jsx
<IndeterminateBar label="Reading your leagues" />
```

One per view, never one per panel. No spinners, no skeletons, no shimmer: `animate-pulse` is banned by name in the app's drift guard. Requires `tokens/base.css`, which declares the keyframes.
